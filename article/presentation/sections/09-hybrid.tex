% =====================================================================
% Слайд 9 — Baseline 4: Hybrid Fusion (наработка лаборатории)
% =====================================================================
\begin{frame}{Baseline 4: Hybrid Fusion}
  \small

  \texttt{HybridScoreFollower} --- HSMM $+$ OLTW $+$ \rlhi{якорный resync}.

  \vspace{0.4em}
  \centering
  \includegraphics[width=0.93\textwidth]{figures/png/hybrid_fusion.png}

  \vspace{0.6em}
  \begin{columns}[T,onlytextwidth]
    \column{0.50\textwidth}
    \footnotesize
    \textbf{Правило выбора:}
    \begin{itemize}\setlength{\itemsep}{0.2em}
      \item $\text{conf} > \theta \Rightarrow$ HSMM;
      \item иначе $\Rightarrow$ OLTW;
      \item якорь нашёл сильное совпадение
            $\Rightarrow$ ресинхронизация обоих.
    \end{itemize}

    \column{0.50\textwidth}
    \footnotesize
    \textbf{Anchor search} ищет последние $K$ событий как окно в score
    через template-matching по pitch $+$ temp-fitting.

    \vspace{0.3em}
    \textbf{Это «классический трекер»} в смысле тезисов --- поверх
    него ставится RL-слой.
  \end{columns}
\end{frame}
