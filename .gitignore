# Build artefacts (LaTeX)
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.synctex
*.synctex.gz.sum.synctex
*.lof
*.lot
*.nav
*.snm
*.vrb

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Python (research code, app code, notebooks)
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
**/.venv/
.vendor/
**/.vendor/

# Local raw template snapshot - kept locally only
ismir-template-raw/

# Local extracted text dumps
musictech_text.txt

# Pixi / poetry / pip lockfiles produced in the venv
pip-wheel-metadata/
*.egg-info/

# NB: article/main.pdf IS tracked so collaborators can preview the
# current state of the paper directly on GitHub without compiling.
